{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test:health": "curl.exe http://localhost:4000/api/health",
    "test:email": "curl.exe -v -X POST http://localhost:4000/api/send-email/verify -H \"Content-Type: application/json\" --data-binary \"@test-email.json\" --fail --show-error",
    "test:email:link": "curl.exe -v -X POST http://localhost:4000/api/send-email/verify -H \"Content-Type: application/json\" --data-binary \"@test-email-link.json\" --fail --show-error",
    "test:email:both": "curl.exe -v -X POST http://localhost:4000/api/send-email/verify -H \"Content-Type: application/json\" --data-binary \"@test-email-both.json\" --fail --show-error"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "resend": "^6.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
